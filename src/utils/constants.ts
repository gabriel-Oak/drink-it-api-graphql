export const COCKTAIL_API = process.env.COCKTAIL_API as string;

export const TRANSLATE_API = process.env.TRANSLATE_API as string;
export const TRANSLATE_API_KEY = process.env.TRANSLATE_API_KEY as string;

export const { POSTGRE_HOST } = process.env;
export const { POSTGRE_USER } = process.env;
export const { POSTGRE_PASS } = process.env;
export const { POSTGRE_PORT } = process.env;
export const { POSTGRE_DB } = process.env;

export const { REDIS_PORT } = process.env;
export const { REDIS_HOST } = process.env;
export const { REDIS_PASS } = process.env;

export const JWT_SECRET = process.env.JWT_SECRET!;
